# Telegram Bot Token from BotFather
BOT_TOKEN=""

# GitHub Personal Access Token (PAT) with 'repo' scope
# This token is used to commit and push subscription files to the target repository.
GITHUB_TOKEN=""

# Default GitHub repository where subscription files will be pushed
# Format: your_username/your_repository_name
# This is used if a user does not configure a specific repository via bot commands (if feature implemented).
DEFAULT_REPO=""

# Optional: Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL="INFO"
